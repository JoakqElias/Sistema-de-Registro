# Cliente React

Desde esta carpeta: `npm.cmd install` y `npm.cmd run dev`.
El backend debe estar iniciado en otra terminal desde `Server`.
Consultar `../Readme.md` para instrucciones completas. El proxy de Vite envía `/api` a `http://127.0.0.1:3000`.
